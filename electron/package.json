{
  "name": "arclight",
  "version": "1.0.0",
  "description": "ArcLight - Data Center Submittal Review Platform",
  "main": "main.js",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "node scripts/build-backend.js && electron-builder",
    "publish": "node scripts/build-backend.js && electron-builder --publish always",
    "postinstall": "electron-builder install-app-deps"
  },
  "dependencies": {
    "tree-kill": "^1.2.2",
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "wait-on": "^8.0.0"
  },
  "build": {
    "appId": "com.positron.arclight",
    "productName": "ArcLight",
    "directories": {
      "output": "../dist/electron"
    },
    "files": [
      "main.js",
      "preload.js",
      "lib/**/*",
      "node_modules/tree-kill/**/*"
    ],
    "extraResources": [
      {
        "from": "../dist/backend",
        "to": "backend",
        "filter": ["**/*"]
      }
    ],
    "publish": {
      "provider": "github",
      "owner": "chrisrwems-blip",
      "repo": "psychic-adventure"
    },
    "win": {
      "target": ["nsis"],
      "icon": "build/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
